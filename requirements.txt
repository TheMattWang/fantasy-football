# Fantasy draft agent v2
#
# Deliberately no torch. The 2025 "GPU MCTS trainer" never trained anything
# (its policy loss was literally torch.tensor(0.0)), and the season simulation
# this replaces it with is a 54 MB numpy array evaluated in ~0.3 ms. There is
# no GPU-scale problem here.
#
# numpy is pinned <2 because several wheels in this stack still ship
# NumPy-1.x-compiled extensions.

pandas>=2.0
numpy>=1.24,<2
scipy>=1.10
scikit-learn>=1.3
pyarrow>=14           # no direct import; pandas.read_parquet needs an engine
requests>=2.31
rapidfuzz>=3.0        # player-name joins
yfpy>=16              # Yahoo Fantasy Sports API
pytest>=8.0

# Removed 2026-08-16: lxml (pandas.read_html is not used anywhere) and
# python-dotenv (yahoo_league parses the cache .env by hand, so that it can
# chmod 600 the file it writes an OAuth refresh token into).
